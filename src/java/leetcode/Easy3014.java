package leetcode;

import java.util.HashMap;
import java.util.Map;

public class Easy3014 {
    public int minimumPushes(String word) {
        if (word.length() <= 8){
            return word.length();
        }
        Map<Integer,String> map = new HashMap<>();
        return 0;
    }
}
